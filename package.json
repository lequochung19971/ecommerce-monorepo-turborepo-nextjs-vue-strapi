{
  "name": "nextjs-strapi",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "build:cms": "npm workspace cms build",
    "build:ecommerce-app": "npm workspace ecommerce-app build",
    "dev": "turbo run dev",
    "start:cms": "npm run -w cms start",
    "start:ecommerce-app": "npm run -w ecommerce-app start",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint-config-custom": "*",
    "prettier": "latest",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18.16.0"
  },
  "dependencies": {},
  "packageManager": "npm@8.3.1"
}
